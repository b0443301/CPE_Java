import java.util.Scanner;

public class main {
	public static void main(String args[]) {
		int run = 0, mod = 0;
		int[] arr;

		Scanner scanner = new Scanner(System.in);
		run = scanner.nextInt();
	    for(int i=0;i<run;i++){
	    	output=scanner.hasNextLine(arr[i]);
	    }
	}
}
